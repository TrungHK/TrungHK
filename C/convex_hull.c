#include <stdio.h>

typedef struct P{
    int l, w, h;
}P;

int main(){
    int n;
    scanf ("%d", &n);
    P max[100];
    int vmax[100]
    int case[100], v[100][100];
    P A[100][100];
    for (int i=0; i<n; i++){
        scanf ("%d %d %d", &max[i].l, &max[i].w, &max[i].h);
        vmax[i]=max[i].l*max[i].w*max[i].h;
        scanf("%d", &case[i]);
        for (int j=0; j<case[i]; j++){
            scanf ("%d %d %d", &A[i][j].l, &A[i][j].w, &A[i][j].h);
            v[i][j]=A[i][j].l*A[i][j].w*A[i][j].h;
        }
    }

    int main(){
        int n;
        scanf ("%d", &n)
    }
    


}